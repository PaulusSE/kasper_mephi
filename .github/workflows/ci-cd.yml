name: Go Application CI/CD

on:
  push:
    branches:
      - master
      - dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - environment: dev
            compose_file: docker-compose-dev.yaml
            branch: dev
          - environment: prod
            compose_file: docker-compose-prod.yaml
            branch: master

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to DockerHub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/app:${{ matrix.environment }}-${{ github.sha }}
        file: Dockerfile
        build-args: |
          ENV=${{ matrix.environment }}

    - name: Set environment secrets
      run: |
        echo "HOST=${{ secrets['SERVER_HOST_prod'] }}" >> $GITHUB_ENV
        echo "USERNAME=${{ secrets['SERVER_USER_prod'] }}" >> $GITHUB_ENV
        echo "PASSWORD=${{ secrets['SERVER_PASSWORD_prod'] }}" >> $GITHUB_ENV
        echo "PORT=${{ secrets['SERVER_PORT_prod'] }}" >> $GITHUB_ENV

    - name: Deploy to Server
      env:
        HOST: ${{ secrets['SERVER_HOST_prod'] }}
        USERNAME: ${{ secrets['SERVER_USER_prod'] }}
        PASSWORD: ${{ secrets['SERVER_PASSWORD_prod'] }}
        PORT: ${{ secrets['SERVER_PORT_prod'] }}
      uses: appleboy/ssh-action@master
      with:
        host: ${{ env.HOST }}
        username: ${{ env.USERNAME }}
        password: ${{ env.PASSWORD }}
        port: ${{ env.PORT }}
        script: |
          pwd
          ls -la
          cp ${{ matrix.compose_file }} ./
          docker pull ${{ secrets.DOCKER_USERNAME }}/app:${{ matrix.environment }}-${{ github.sha }}
          docker-compose -f ${{ matrix.compose_file }} down
          docker-compose -f ${{ matrix.compose_file }} up -d
